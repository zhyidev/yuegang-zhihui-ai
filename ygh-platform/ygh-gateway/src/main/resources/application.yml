server:
  port: 8080

spring:
  application:
    name: ygh-gateway
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
    gateway:
      discovery:
        locator:
          enabled: true

ygh:
  gateway:
    cors:
      allowed-origins: "*"
