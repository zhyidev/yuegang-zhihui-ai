meta { name: Password Reset Request type: http seq: 4 }
post { url: {{baseUrl}}/api/v1/auth/password-reset/request body: json auth: none }
body:json { {"principal":"demo@example.com","captchaChallengeId":"{{captchaChallengeId}}","captchaAnswer":"{{captchaAnswer}}"} }
tests { test("anti-enumeration response", function () { expect(res.body.data).to.have.property("expiresIn"); }); }
