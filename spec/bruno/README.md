# Bruno 后端联调集合

集合按 `01-auth → 02-mall → 03-ai → 04-training` 执行，覆盖登录、虚拟充值下单、模拟支付、知识发布问答和培训闯关的核心请求。所有密码、验证码、重置令牌、文件路径和业务 ID 只在本地环境变量中填写，不写入仓库。

当前文件属于实现冻结交付物；集中测试阶段将启动对应场景服务，修正运行实例导出的最终字段，并把通过时间和 Commit SHA 写回接口清单。充值、支付、退款、订单履约均为模拟行为，不产生真实资金或物流。
