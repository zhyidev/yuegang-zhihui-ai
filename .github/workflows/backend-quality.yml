name: backend-quality

on:
  workflow_dispatch:
  pull_request:
    paths: ["pom.xml", "ygh-*/**", "spec/**", ".github/workflows/backend-quality.yml"]
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "25"
          cache: maven
      - name: Verify reactor
        run: mvn --batch-mode --no-transfer-progress clean verify
      - name: Generate CycloneDX SBOM
        run: mvn --batch-mode --no-transfer-progress -Prelease-sbom package -DskipTests
      - name: Scan repository dependencies and configuration
        # v0.36.0; pin the verified commit to reduce third-party action supply-chain risk.
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25
        with:
          scan-type: fs
          scan-ref: .
          scanners: vuln,misconfig,secret,license
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          exit-code: 1
      - uses: actions/upload-artifact@v7
        with:
          name: backend-verification
          path: |
            **/target/surefire-reports/**
            **/target/failsafe-reports/**
            **/target/site/jacoco/**
            target/bom.*
          if-no-files-found: warn
  compose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Validate compose manifests
        env:
          YGH_VERSION: ci
        run: docker compose --env-file ygh-deploy/enterprise-server/.env.example -f ygh-deploy/enterprise-server/compose.yml config --quiet
